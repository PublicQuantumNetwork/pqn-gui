"use client";

import React from 'react';
import { Typography, Box } from '@mui/material';

const QKDPage = () => {
  return (
    <Box
      sx={{
        display: 'flex',
        justifyContent: 'center',
        alignItems: 'center',
        height: '100vh',
        color: "black"
      }}
    >
      <Typography variant="h2" >
        Welcome to QKD 🧑‍🔬
      </Typography>
    </Box>
  );
};

export default QKDPage;
